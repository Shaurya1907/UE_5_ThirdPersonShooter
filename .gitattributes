# Unreal Engine Binary Assets (Critical for Git LFS)

*.uasset filter=lfs diff=lfs merge=lfs -text
*.umap filter=lfs diff=lfs merge=lfs -text

# Ensure source code and project files remain as standard text

*.ini text
*.uplugin text
*.uproject text
*.cs text
*.h text
*.cpp text
*.config text
*.xml text
*.md text
*.txt text
*.json text
*.gitignore text
*.gitattributes text
